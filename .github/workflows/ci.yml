name: macOS CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6
      - name: Install XcodeGen
        run: brew install xcodegen
      - name: Run deterministic OCR fallback smoke
        run: make smoke-ocr-fallback
      - name: Run CI
        run: make ci-cloud
      - name: Run cleanup review UI tests
        run: make ui-test-cleanup-review
        timeout-minutes: 15
      - name: Build Release for Artifact
        run: make build
      - name: Upload app artifact
        uses: actions/upload-artifact@v7
        with:
          name: PDFQuickFix.app
          path: build/Build/Products/Release/PDFQuickFix.app
